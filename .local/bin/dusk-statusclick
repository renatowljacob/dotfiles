#!/bin/sh

case $BLOCK_STATUS in
0) statusbar-quitclick ;;
1) statusbar-calendarclick ;;
4) statusbar-volumeclick ;;
5) statusbar-updatesclick ;;
6) statusbar-spotifyclick ;;
*) dunstify -a dusk -t 2000 "No action set" ;;
esac
